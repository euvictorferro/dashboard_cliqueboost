# Página "Atas" Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar uma página "Atas" ao dashboard de cada cliente, mostrando o histórico de atas de calls já feitas com a Clique Boost — view-only, sem agendamento/remarcação.

**Architecture:** Nova tabela Supabase `call_notes` (RLS sem policies, acesso só via Service Role Key) + camada de dados `src/lib/callNotes.ts` + rota `GET /api/atas/[client]` (mesmo padrão de auth de todas as outras rotas) + página cliente com Sidebar atualizada (item novo "Atas", fora do grupo Social Media) + lista em accordion (clique expande/recolhe o texto da ata).

**Tech Stack:** Next.js 16 App Router, TypeScript, Tailwind v4, Supabase (`@supabase/supabase-js`) — sem dependências novas.

## Global Constraints

- Escrita de atas não é escopo deste plano — não existe formulário de admin nem endpoint POST. A tabela só é populada manualmente (pelo Victor, via Claude Code numa conversa, direto no Supabase) depois que este plano estiver implantado.
- Não inserir nenhuma ata de exemplo/mock permanente durante a implementação — qualquer linha de teste inserida pra verificar a Task 1/3 deve ser removida ao final da mesma task.
- Cada ata é um bloco de texto livre (`title` + `call_date` + `content`), sem campos estruturados separados.
- Página fica fora do grupo "Social Media" da Sidebar — é um item standalone, junto com Dashboard/Tasks.
- Mesmo padrão de autenticação por token (`verifyClientToken`) de todas as outras rotas/páginas client-facing deste projeto.
- Sem suíte de testes automatizada neste projeto (padrão já estabelecido) — verificação via `npx tsc --noEmit`, `npm run build`, inserção real de dados de teste via `curl` (removidos ao final), e checagem visual no Browser pane.
- Não mesclar em `main`/`staging` sem aprovação explícita do Victor.

## Handoff obrigatório antes da Task 1

Este plano cria uma migration nova (`supabase/migrations/0004_call_notes.sql`). O ambiente deste agente **não tem acesso de aplicar migrations** (só `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` em `.env.local`, sem connection string Postgres nem Supabase CLI linkado). Antes de começar a Task 1:

1. O implementador da Task 1 deve **parar e avisar o controller** assim que criar o arquivo da migration, pedindo pro Victor rodar o SQL no SQL Editor do Supabase (mesmo fluxo já usado nas migrations `0001`/`0002`/`0003` deste projeto).
2. Só depois de o Victor confirmar que rodou o SQL, o implementador continua pro resto da Task 1 (verificação real via curl).

---

### Task 1: Migration `call_notes` + `src/lib/callNotes.ts`

**Files:**
- Create: `supabase/migrations/0004_call_notes.sql`
- Create: `src/lib/callNotes.ts`

**Interfaces:**
- Produces: `CallNote = { id: string; title: string; callDate: string; content: string }`, `fetchCallNotes(clientId: string): Promise<CallNote[]>` — exportados de `src/lib/callNotes.ts`, consumidos pela Task 2 (rota de API) e pela Task 3 (tipo usado nos componentes de UI).

- [ ] **Step 1: Criar a migration `supabase/migrations/0004_call_notes.sql`**

```sql
create table if not exists call_notes (
  id uuid primary key default gen_random_uuid(),
  client_id text not null,
  title text not null,
  call_date date not null,
  content text not null,
  created_at timestamptz not null default now()
);

-- RLS ligado e sem policies: só a Service Role Key (usada no servidor) acessa esta tabela.
alter table call_notes enable row level security;
```

- [ ] **Step 2: PARAR e pedir handoff**

Avise o controller/Victor: "Criei a migration `0004_call_notes.sql`. Preciso que você rode esse SQL no SQL Editor do Supabase antes de eu continuar a verificação real desta task." Espere a confirmação antes de prosseguir pro Step 3.

- [ ] **Step 3: Criar `src/lib/callNotes.ts`**

```ts
// src/lib/callNotes.ts
// ponytail: server-only — nunca importar isto de um componente "use client" (usa a Service Role Key).
import { getSupabaseAdmin } from "./supabase";

export type CallNote = { id: string; title: string; callDate: string; content: string };

export async function fetchCallNotes(clientId: string): Promise<CallNote[]> {
  const supabase = getSupabaseAdmin();
  if (!supabase) throw new Error("Supabase não configurado");
  const { data, error } = await supabase
    .from("call_notes")
    .select("id, title, call_date, content")
    .eq("client_id", clientId)
    .order("call_date", { ascending: false });
  if (error) throw new Error(error.message);
  return (data ?? []).map((row) => ({
    id: row.id,
    title: row.title,
    callDate: row.call_date,
    content: row.content,
  }));
}
```

- [ ] **Step 4: Verificar tipos**

Run: `npx tsc --noEmit`
Expected: zero erros.

- [ ] **Step 5: Verificação real com dados de teste (inseridos e depois removidos)**

Depois da confirmação do Victor de que a migration rodou, insira 2 linhas de teste via `curl` contra a API REST do Supabase (mesmo padrão já usado neste projeto pra testar `content_competitors`), usando `client_id: "tiago"` (cliente de teste, sem risco de poluir dado real de outro cliente):

```bash
set -a && source .env.local && set +a
curl -s -X POST "$SUPABASE_URL/rest/v1/call_notes" \
  -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" \
  -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" \
  -H "Content-Type: application/json" \
  -H "Prefer: return=representation" \
  -d '[
    {"client_id": "tiago", "title": "Call de teste 1 (verificação Task 1)", "call_date": "2026-07-20", "content": "Conteúdo de teste 1."},
    {"client_id": "tiago", "title": "Call de teste 2 (verificação Task 1)", "call_date": "2026-07-25", "content": "Conteúdo de teste 2."}
  ]'
```

Anote os `id`s retornados (vai precisar deles no Step 7 pra apagar).

- [ ] **Step 6: Rodar um script Node isolado chamando `fetchCallNotes`**

```bash
set -a && source .env.local && set +a
node -e "
const { createClient } = require('@supabase/supabase-js');
const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY, { auth: { persistSession: false } });
(async () => {
  const { data, error } = await supabase.from('call_notes').select('id, title, call_date, content').eq('client_id', 'tiago').order('call_date', { ascending: false });
  if (error) { console.error('FAIL', error.message); process.exit(1); }
  console.log(JSON.stringify(data, null, 2));
  if (data.length !== 2 || data[0].call_date !== '2026-07-25') { console.error('FAIL: ordem ou quantidade errada'); process.exit(1); }
  console.log('OK: 2 registros, ordenados por call_date decrescente');
})();
"
```

Expected: imprime os 2 registros de teste com `2026-07-25` primeiro (mais recente), e a linha final `OK: 2 registros, ordenados por call_date decrescente`.

- [ ] **Step 7: Remover as linhas de teste**

```bash
set -a && source .env.local && set +a
curl -s -X DELETE "$SUPABASE_URL/rest/v1/call_notes?client_id=eq.tiago" \
  -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" \
  -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY"
```

Confirme que sumiu rodando de novo o script do Step 6 (ajustado pra aceitar 0 resultados) ou um `curl -X GET` simples — não deve sobrar nenhuma linha com `client_id=eq.tiago` na tabela `call_notes`.

- [ ] **Step 8: Build**

Run: `npm run build`
Expected: build limpo (arquivo novo ainda não é importado por nenhuma rota).

- [ ] **Step 9: Commit**

```bash
git add supabase/migrations/0004_call_notes.sql src/lib/callNotes.ts
git commit -m "Adiciona migration call_notes + camada de dados src/lib/callNotes.ts"
```

---

### Task 2: Rota `GET /api/atas/[client]`

**Files:**
- Create: `src/app/api/atas/[client]/route.ts`

**Interfaces:**
- Consumes: `fetchCallNotes(clientId)` de `src/lib/callNotes.ts` (Task 1), `CLIENTS` de `src/lib/clients.ts`, `verifyClientToken` de `src/lib/access.ts`.
- Produces: `GET /api/atas/[client]?key=TOKEN` → `{ notes: CallNote[] }` em caso de sucesso — consumido pela Task 3.

- [ ] **Step 1: Criar `src/app/api/atas/[client]/route.ts`**

```ts
// src/app/api/atas/[client]/route.ts
import { NextRequest } from "next/server";
import { CLIENTS } from "@/lib/clients";
import { fetchCallNotes } from "@/lib/callNotes";
import { verifyClientToken } from "@/lib/access";

export async function GET(request: NextRequest, { params }: { params: Promise<{ client: string }> }) {
  const { client: clientId } = await params;
  const key = request.nextUrl.searchParams.get("key") ?? undefined;

  const client = CLIENTS.find((c) => c.id === clientId);
  if (!client) return Response.json({ error: "unknown client" }, { status: 404 });
  if (!(await verifyClientToken(clientId, key))) return Response.json({ error: "unauthorized" }, { status: 401 });

  try {
    const notes = await fetchCallNotes(clientId);
    return Response.json({ notes });
  } catch (err) {
    console.error(`[atas] falha ao buscar atas de ${clientId}:`, err);
    return Response.json({ error: "fetch_failed" }, { status: 502 });
  }
}
```

- [ ] **Step 2: Verificar tipos**

Run: `npx tsc --noEmit`
Expected: zero erros.

- [ ] **Step 3: Build**

Run: `npm run build`
Expected: build limpo, `ƒ /api/atas/[client]` presente na lista de rotas.

- [ ] **Step 4: Verificação real ao vivo — 401/404/200**

Com o dev server rodando (`.claude/launch.json`, config `dashboard-cliqueboost`, porta 3001):

```bash
# 404: cliente inexistente
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:3001/api/atas/naoexiste?key=qualquer"
# esperado: 404

# 401: cliente real, token errado
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:3001/api/atas/tiago?key=chaveerrada"
# esperado: 401

# 200: cliente real, token certo (mesmo token do Tiago usado nas verificações anteriores deste projeto)
curl -s "http://localhost:3001/api/atas/tiago?key=b9d179192160c98b579807d25f8a956e"
# esperado: {"notes":[]}  (tabela vazia nesse momento, já que as linhas de teste da Task 1 foram removidas)
```

Expected: os 3 códigos batem exatamente com o comentado, e o `GET` de sucesso retorna `{"notes":[]}`.

- [ ] **Step 5: Commit**

```bash
git add src/app/api/atas/[client]/route.ts
git commit -m "Adiciona rota GET /api/atas/[client]"
```

---

### Task 3: Sidebar + página + lista de atas (UI completa)

**Files:**
- Modify: `src/components/Sidebar.tsx`
- Create: `src/app/[client]/atas/page.tsx`
- Create: `src/components/AtasPageClient.tsx`
- Create: `src/components/AtasList.tsx`

**Interfaces:**
- Consumes: `CallNote` de `src/lib/callNotes.ts` (Task 1), rota `GET /api/atas/[client]` (Task 2).
- Produces: rota `/[client]/atas` navegável a partir da Sidebar.

- [ ] **Step 1: Modificar `src/components/Sidebar.tsx` — adicionar item "Atas"**

Adicione o ícone novo logo depois de `function TasksIcon() { ... }` (linha 26 do arquivo atual):

```tsx
function AtasIcon() {
  return (
    <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true">
      <rect x="3" y="2" width="12" height="14" rx="1.5" stroke="currentColor" strokeWidth="1.5" />
      <path d="M6 6h6M6 9h6M6 12h3.5" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" />
    </svg>
  );
}
```

Troque a linha `type ActiveKey = "dashboard" | "tasks" | "conteudos" | "calendario" | "bunker";` por:

```tsx
type ActiveKey = "dashboard" | "tasks" | "atas" | "conteudos" | "calendario" | "bunker";
```

Troque o array `STANDALONE_ITEMS` (atual: só `Dashboard` e `Tasks`) por:

```tsx
const STANDALONE_ITEMS: NavItemDef[] = [
  { href: "", label: "Dashboard", key: "dashboard", icon: DashboardIcon },
  { href: "/tasks", label: "Tasks", key: "tasks", icon: TasksIcon },
  { href: "/atas", label: "Atas", key: "atas", icon: AtasIcon },
];
```

Nada mais no arquivo muda — `SOCIAL_MEDIA_ITEMS`, `SOCIAL_MEDIA_KEYS`, `NavLink`, e o resto de `Sidebar` continuam exatamente iguais.

- [ ] **Step 2: Criar `src/app/[client]/atas/page.tsx`**

```tsx
// src/app/[client]/atas/page.tsx
import { notFound } from "next/navigation";
import { CLIENTS } from "@/lib/clients";
import { AccessDenied } from "@/components/AccessDenied";
import { Sidebar } from "@/components/Sidebar";
import { AtasPageClient } from "@/components/AtasPageClient";
import { verifyClientToken } from "@/lib/access";

export default async function ClientAtasPage({
  params,
  searchParams,
}: {
  params: Promise<{ client: string }>;
  searchParams: Promise<{ key?: string }>;
}) {
  const { client } = await params;
  const { key } = await searchParams;
  const found = CLIENTS.find((c) => c.id === client);
  if (!found) notFound();

  const authorized = await verifyClientToken(found.id, key);
  if (!authorized) return <AccessDenied />;

  return (
    <div className="flex min-h-full">
      <Sidebar clientId={found.id} accessKey={key!} active="atas" />
      <div className="min-w-0 flex-1">
        <AtasPageClient clientId={found.id} accessKey={key!} />
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Criar `src/components/AtasPageClient.tsx`**

```tsx
// src/components/AtasPageClient.tsx
"use client";

import { useEffect, useState } from "react";
import type { CallNote } from "@/lib/callNotes";
import { AtasList } from "./AtasList";

export function AtasPageClient({ clientId, accessKey }: { clientId: string; accessKey: string }) {
  const [notes, setNotes] = useState<CallNote[] | null>(null);
  const [error, setError] = useState(false);

  useEffect(() => {
    let cancelled = false;
    setNotes(null);
    setError(false);
    fetch(`/api/atas/${clientId}?key=${encodeURIComponent(accessKey)}`)
      .then(async (res) => {
        const data = await res.json();
        if (!res.ok) throw new Error();
        return data as { notes: CallNote[] };
      })
      .then((data) => {
        if (!cancelled) setNotes(data.notes);
      })
      .catch(() => {
        if (!cancelled) setError(true);
      });
    return () => {
      cancelled = true;
    };
  }, [clientId, accessKey]);

  return (
    <div className="mx-auto w-full max-w-[1600px] px-6 py-10 sm:px-10">
      <h1 className="mb-6 text-2xl font-bold text-foreground">Atas</h1>
      {error && (
        <p className="rounded-[var(--radius-card)] bg-card p-6 text-center text-sm text-muted-foreground shadow-[var(--shadow-soft)]">
          Não foi possível carregar as atas agora.
        </p>
      )}
      {!error && !notes && <p className="text-sm text-muted-foreground">Carregando...</p>}
      {!error && notes && <AtasList notes={notes} />}
    </div>
  );
}
```

- [ ] **Step 4: Criar `src/components/AtasList.tsx`**

```tsx
// src/components/AtasList.tsx
"use client";

import { useState } from "react";
import type { CallNote } from "@/lib/callNotes";

// ponytail: call_date vem como "YYYY-MM-DD" (tipo date do Postgres) — parse manual em vez de
// `new Date(callDate)` pra não sofrer com fuso horário (meia-noite UTC vira dia anterior em
// alguns fusos locais).
function formatCallDate(callDate: string): string {
  const [year, month, day] = callDate.split("-");
  return `${day}/${month}/${year}`;
}

export function AtasList({ notes }: { notes: CallNote[] }) {
  const [expandedId, setExpandedId] = useState<string | null>(null);

  if (notes.length === 0) {
    return (
      <div className="rounded-[var(--radius-card)] bg-card p-8 text-center shadow-[var(--shadow-soft)]">
        <p className="text-sm text-muted-foreground">Nenhuma ata registrada ainda.</p>
      </div>
    );
  }

  return (
    <div className="space-y-3">
      {notes.map((note) => {
        const isExpanded = expandedId === note.id;
        return (
          <div key={note.id} className="rounded-[var(--radius-card)] bg-card shadow-[var(--shadow-soft)]">
            <button
              type="button"
              onClick={() => setExpandedId(isExpanded ? null : note.id)}
              className="flex w-full items-center justify-between gap-3 px-5 py-4 text-left"
            >
              <div>
                <p className="text-sm font-bold text-card-foreground">{note.title}</p>
                <p className="text-xs text-muted-foreground">{formatCallDate(note.callDate)}</p>
              </div>
              <span className={`shrink-0 text-muted-foreground transition-transform ${isExpanded ? "rotate-180" : ""}`}>
                ▾
              </span>
            </button>
            {isExpanded && (
              <div className="border-t border-border px-5 py-4">
                <p className="whitespace-pre-wrap text-sm text-card-foreground">{note.content}</p>
              </div>
            )}
          </div>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 5: Verificar tipos**

Run: `npx tsc --noEmit`
Expected: zero erros.

- [ ] **Step 6: Build**

Run: `npm run build`
Expected: build limpo, `ƒ /[client]/atas` presente na lista de rotas.

- [ ] **Step 7: Verificação real com dados de teste (inseridos e depois removidos)**

Insira 2 linhas de teste de novo (mesmo client_id `tiago`, mesmo comando do Step 5 da Task 1) pra ter algo real pra ver na tela:

```bash
set -a && source .env.local && set +a
curl -s -X POST "$SUPABASE_URL/rest/v1/call_notes" \
  -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" \
  -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" \
  -H "Content-Type: application/json" \
  -H "Prefer: return=representation" \
  -d '[
    {"client_id": "tiago", "title": "Alinhamento mensal de conteúdo", "call_date": "2026-07-10", "content": "Discutimos o calendário de julho e ajustamos o pilar de autoridade local.\n\nPróximos passos: aumentar frequência de Reels."},
    {"client_id": "tiago", "title": "Kickoff da campanha de verão", "call_date": "2026-07-22", "content": "Definimos os temas da campanha de verão e os formatos prioritários."}
  ]'
```

- [ ] **Step 8: Checagem visual completa**

Abrir `/tiago/atas?key=b9d179192160c98b579807d25f8a956e`. Confirmar:
- Item "Atas" aparece na Sidebar, entre "Tasks" e o grupo "Social Media", com o ícone certo, e fica destacado (`active`) quando a página está aberta.
- As 2 atas de teste aparecem na lista, a mais recente ("Kickoff da campanha de verão", 22/07/2026) primeiro.
- Clicar num card expande o texto completo (`whitespace-pre-wrap` preservando a quebra de linha do exemplo); clicar de novo recolhe.
- Navegar pra outra página (ex: Tasks) e voltar pra Atas — sidebar continua com "Atas" ativo certo.
- `read_console_messages` sem erros.

- [ ] **Step 9: Remover as linhas de teste**

```bash
set -a && source .env.local && set +a
curl -s -X DELETE "$SUPABASE_URL/rest/v1/call_notes?client_id=eq.tiago" \
  -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" \
  -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY"
```

Recarregue `/tiago/atas?key=b9d179192160c98b579807d25f8a956e` e confirme que volta a mostrar "Nenhuma ata registrada ainda." — a tabela `call_notes` não deve ter nenhuma linha real ou de teste ao final desta task.

- [ ] **Step 10: Commit**

```bash
git add src/components/Sidebar.tsx src/app/[client]/atas/page.tsx src/components/AtasPageClient.tsx src/components/AtasList.tsx
git commit -m "Adiciona página Atas: Sidebar, rota, e lista em accordion"
```
